import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    modalContent:{
        padding: 20,
    },
    mainContainer:{
        backgroundColor: '#2c2e40',
    },
    container:{
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
    },
    host:{ backgroundColor: '#000'},
    text:{fontSize:20, color:'white'},

});

export default styles;
